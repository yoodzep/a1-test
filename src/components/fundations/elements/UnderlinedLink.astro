---
const { variant = "dark", class: additionalClass, href } = Astro.props;
---

<a
  href={href}
  class={`flex text-lg w-fit font-medium
    ${
      variant === "dark"
        ? "text-accent-900 hover:text-accent-500 after:bg-accent-500 hover:after:bg-accent-500"
        : "text-white hover:text-base-300 after:bg-base-300 hover:after:bg-base-300"
    }
    relative
    after:absolute after:left-0 after:-bottom-1
    after:h-0.5 after:w-0
    after:transition-[width] after:duration-300 after:ease-out
    hover:after:w-full ${additionalClass || ""}`}
>
  <slot />
</a>
